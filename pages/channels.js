import { Box, Typography } from '@mui/material';
import { PageWrapper } from '../components/page-wrapper';

const ChannelsPage = () => {
    return (
        <PageWrapper>
            <Typography>This is Channels</Typography>
        </PageWrapper>
    );
};

export default ChannelsPage;
